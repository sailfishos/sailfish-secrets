[D-BUS Service]
Name=org.sailfishos.secrets.daemon.discovery
# make sure this is never used as we want systemd to start with user:privileged, not just user
Exec=/bin/false
SystemdService=sailfish-secretsd.service
